{
  "core_proxy": {
    "mappings": {

      "rev_proxy": {
        "dynamic": "strict",
        "properties": {
          "inc_url":            {"type": "string"},
          "out_url":            {"type": "string"},
          "auth_method":        {"type": "string"},
          "header":             {"type": "string"}
        }
      },

      "public": {
        "dynamic": "strict",
        "properties": {
          "url":            {"type": "string"}
        }
      }

    }
  }
}
